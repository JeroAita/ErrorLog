# Repo multi-app: backend/ y frontend/ mantienen sus propios .gitignore
# con las reglas específicas (Rails / React). Acá sólo reglas de raíz.

# Planes / artefactos generados por los agentes de IA (documentan sesiones,
# no forman parte del código versionado)
.opencode/
